<header>
  <!-- Fixed navbar -->
  <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <a class="navbar-brand" id="logo" href="/">RailsApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="col-md-8"></div>
    <div class="collapse navbar-collapse  col-md-4" id="navbarCollapse">
      <ul class="navbar-nav">
        <li class="nav-item
          <% if @title == "Home" %>active
          <% end %>">
          <a class="nav-link" href="/">Home</a>
        </li>
        <li class="nav-item
          <% if @title == "Help" %>active
          <% end %>">
          <a class="nav-link" href="<%= help_path %>">Help</a>
        </li>
        <li class="nav-item
          <% if @title == "Login" %>active
          <% end %>">
          <a class="nav-link" href="#">Login</a>
        </li>
      </ul>
    </div>
  </nav>
</header>